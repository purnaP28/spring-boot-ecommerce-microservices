pipeline {
     agent any
     stages{
        stage ('checkout') {
             steps{
               checkout scm
             }
         }
        stage ('build') {
             steps{
                sh "mvn -f order-serivce/pom.xml clean install"
             }
         }   
      }
}
